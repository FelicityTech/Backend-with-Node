const moment = require('moment')

const now = moment()
console.log(now.format('YYYY-MM-DD HH:mm:ss'));
// This program uses the moment module to get the 
// current date and time and format it as a string